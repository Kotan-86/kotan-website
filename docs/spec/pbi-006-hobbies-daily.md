# PBI-006: 内定/入社の同期はこーたんの私的な一面を知れて話してみたいと思える

## Issue

#7 — https://github.com/Kotan-86/kotan-website/issues/7

スプリントバックログ上の表記 `PBI-006` は、この issue #7 を指す。

**承認状態: 承認済み(2026-09-24)。** 2026-09-24 起票。質問 Q1〜Q4 は、2026-09-24 に PO が決定した(「PO の決定」節)。同日、PO は、受入基準 AC-1〜AC-21、「spec-designer が具体化した点」、「本PBIが後続PBIへ残す約束」を承認し、PBI-005 AC-4(h2 2つ・h3 10個)の本PBI AC-4(h2 3つ・h3 14個)への置き換えを了承した(AskUserQuestion「承認する」。「既存の承認済み AC への影響」節)。

## 目的(Why)

プロダクトゴール「初対面の人やオンライン上で交流する人が、僕自身の身の丈に合った前向きな印象形成をスムーズに構築できる状態にする」([docs/product-goal.md](../product-goal.md))に対して、現在のページは、ヒーロー、活動歴、僕のコミュニケーション方法で構成されている。

- 大学以降の活動やコミュニケーション方法から、こーたんがどんな活動をしてきた人なのかは分かる。〔出典: issue #7 の Why〕
- 一方、それらは活動や仕事に近い話題が中心で、初対面の同期が気軽に話しかけるための材料にはなりにくい。〔出典: issue #7 の Why〕
- 趣味や日常など当たり障りのない話題から共通点や人となりを知れることで、会話を始めるきっかけを作れるようにする。〔出典: issue #7 の Why〕

## 前提

### 実装の現状(コードの観測事実)

- `src/pages/index.astro`: `main` 要素(`flex flex-col items-center gap-12 px-6 sm:px-10`)の中に、①ヒーロー専用のラッパー要素、②活動歴(`ActivityTimeline`)、③コミュニケーション節(`CommunicationSection`)が、この順に並ぶ。〔確認: 実行(ファイルの読み出し)〕
- **セクション間の余白**は、`main` の `gap-12`(3rem)の1つで決まっている。ヒーローと活動歴の間、活動歴とコミュニケーション節の間は、どちらもこの gap である。活動歴・コミュニケーション節は外側の上の余白を持たない(コミュニケーション節は下の内側余白 `pb-16` を持つ)。〔確認: 実行(ファイルの読み出し)。ブラウザでの表示上の値は spec-designer は観測していない〕
  - PBI-005 の「本PBIが後続PBIへ残す約束」は、セクション間の余白の値を「PO 確認が全 OK になった後の確定版で記録する」としているが、[pbi-005-activity-timeline.md](./pbi-005-activity-timeline.md) にその値の記録は見当たらない(全文の読み出しで確認)。本書では、上記のコードの値を「実装の現状」として扱い、PO が決めた値として前提にはしない。
- ヒーローのラッパー要素の上側の空きは、`pt-[max(3rem,calc(50vh_-_215px))]`(640px 以上は `calc(50vh_-_128px)`)で、画面の高さと固定の見積もり値だけから決まり、ヒーローの後ろに続く内容には依存しない。下側の空きは持たない。〔確認: 実行(ファイルの読み出し)。よって、ヒーローと活動歴の間に新しいセクションを挟んでも、ヒーローの上側の空きは変わらないと読める(未確認: コードを読んだ推測。ブラウザ未観測)〕
- 既存セクションの見た目(〔確認: 実行(ファイルの読み出し)〕):

  | 要素 | 活動歴 | 僕のコミュニケーション方法 |
  | --- | --- | --- |
  | セクションの幅・左端 | `mx-auto w-full max-w-2xl`(672px 相当。ヒーローのブロックと同じ) | 同左 |
  | 見出しとカードの間隔 | `gap-6`(1.5rem) | 同左 |
  | `h2`(カードの外、ページ背景の上) | `text-xl`(640px 以上 `text-2xl`)・`font-bold`・本文色 | 同左 |
  | カード | 白地(`bg-card-background`)・角丸 `rounded-2xl`・内側余白 `p-6`・枠線と影なし。**1枚** | 同左。**サブセクションごとに3枚** |
  | `h3`(カードの中) | 活動名。`text-lg`(640px 以上 `text-xl`)・`font-semibold`・本文色 | サブセクションの見出し。同左 |
  | 本文 | `text-base`・本文色 | 同左(`ul` の `li`、行頭記号あり) |
  | 項目の構造 | `ol` の `li` ごとに 時期・`h3`・本文 | `ul` の `li` が本文の1項目 |

- 既存のビルド成果物 `dist/index.html`(2026-09-24 01:47 更新。spec-designer は再ビルドしていない)の見出しは、文書の順に `h1` 1つ → `h2`「活動歴」→ `h3` 7つ → `h2`「僕のコミュニケーション方法」→ `h3` 3つ である。〔確認: 実行(`grep`)〕
- 色トークンは4つ(`--color-brand-primary: #606da1`、`--color-bg-surface`(`#cfd3e3` 相当)、`--color-text-body: #4f5984`、`--color-card-background: #ffffff`)。〔確認: 実行(ファイルの読み出し)〕
- コントラスト比(WCAG 2.x の相対輝度式): 本文色と白 6.786:1、基調色と白 5.001:1、本文色とページ背景 4.550:1。〔確認: 実行(一時領域のスクリプトで計算)〕
- 文言の定義は、活動歴が `src/data/activities.ts`、コミュニケーション節が `src/data/communication.ts` の各1か所にあり、マークアップに直書きしていない。〔確認: 実行(ファイルの読み出し)〕
- [README.md](../../README.md) には、ページのセクション構成に関する記述が無い。〔確認: 実行(ファイルの読み出し)〕

### POの意図(確認済み)

出典1: GitHub issue #7 の本文(作成者 = PO 本人)。**spec-designer は issue を直接取得していない**(ネットワークにアクセスしないため)。以下は main 経由で受領した本文による。文字種(全角/半角)は main の転記に依存する。

- **Why**: 上記「目的(Why)」のとおり。
- **見出し**: 「趣味・日常」とする。
- **カード**: **1枚の白地カード**内に、下記の4項目を掲載する。
- **文言**: 文字を変えない。
- **位置**: ヒーローの下、活動歴の上に置く。

issue に記された4項目(issue の表記のまま。照合用):

| 順 | issue の表記 |
| --- | --- |
| 1 | スポーツ観戦｜戦術分析で「なぜこのプレーなのか」を理解しながら見ています |
| 2 | 印象派展｜当時の作者が感じたことを想像しながらゆっくり観覧します |
| 3 | のんびり旅｜自然やゆっくりできる場所に行って、景色などを楽しむことが多いです |
| 4 | 読書｜技術書やビジネス教養書などを読んで、試してみることが多いです |

> 「｜」は、main の転記では全角の縦線(U+FF5C)である(一時領域のスクリプトで文字コードを確認)。4項目とも「｜」はちょうど1つで、その前が項目名に当たる語(スポーツ観戦・印象派展・のんびり旅・読書)、後ろが説明文である。説明文の末尾に句点「。」は無い(issue の表記のまま)。
>
> 「｜」を画面に文字として表示するか、項目名と説明文を分ける区切りの記法として扱うかは、issue からは決まらなかった。PO の決定(出典3の Q1)により、区切りの記法として扱い、画面には表示しない。

出典2: 既に合意済みの上位仕様・約束。

- [docs/spec/website.md](./website.md)「ページ構成(セクション)」第6項「**私的な面** … 趣味・興味・話しやすい話題(公開制限は特になし。内容は後日追記)」、「公開目標」(私的な面は簡易版でも可)、「未決・後続」(私的な面の具体トピック)。本PBIの内容を第6項の実装とすることは、出典3の Q4 で PO が決定した。
- [pbi-004-communication-section.md](./pbi-004-communication-section.md)「本PBIが後続PBIへ残す約束」: `h1` は名前の1つだけ、各セクションの見出しは `h2`、その下のまとまりは `h3`。ページ背景の上の見出しに基調色を使わない。カードは `--color-card-background` を参照し、カードの上の文字は本文色。文言はマークアップから分けた1か所の定義に置く。
- [pbi-005-activity-timeline.md](./pbi-005-activity-timeline.md)「本PBIが後続PBIへ残す約束」: 内容のセクションどうしの間の余白は、活動歴とコミュニケーション節の間の余白と同じにする。ヒーローと直後のセクションの間の余白も、それと同じぐらいにする。ヒーローの上側の見え方を保つ。本文より控えめに見せたい文字は、白地のカードの上で基調色を使う。セクションの順序は「ヒーロー → 活動歴 → 僕のコミュニケーション方法」(本PBIは、issue の指示により、ヒーローと活動歴の間に本セクションを挟む)。
- [pbi-002-warm-background-color.md](./pbi-002-warm-background-color.md)「後続PBIのための共有の約束」: トークン名を変えない、`.astro` に16進値を直書きしない、文字と直下の背景のコントラスト比は 4.5:1 以上。
- [pbi-003-hero-photo-and-sns-links.md](./pbi-003-hero-photo-and-sns-links.md)「本PBIが後続PBIへ残す約束」: ヒーローの構成を壊さない。

出典3: PO の決定(2026-09-24。main が AskUserQuestion で PO に提示した Q1〜Q4 への回答。詳細は「PO の決定」節)。

- **Q1(「｜」の表示)**: 項目名と説明文を分けて表示し、「｜」は表示しない。項目名・説明文の文字は1文字も変えない。
- **Q2(項目名の扱い)**: 項目名は見出し `h3` とし、大きさ・太さは活動歴の活動名と同じにする。
- **Q3(並べ方)**: すべての画面幅で縦一列。各項目は、項目名の下に説明文を置く。
- **Q4(website.md での位置づけ)**: website.md 第6項「私的な面」の実装とし、「未決・後続」の「私的な面の具体トピック」を解消済みにする。

## 実現内容(What)

### ユーザーから見えるもの

1. トップページの並びが、上から **ヒーロー → 趣味・日常 → 活動歴 → 僕のコミュニケーション方法** になる。
2. 新しいセクションの見出しは「趣味・日常」で、カードの外(ページ背景の上)に置く。
3. 見出しの下に**白地のカード1枚**があり、その中に4項目が、上から issue の順(スポーツ観戦 → 印象派展 → のんびり旅 → 読書)に並ぶ。
4. 各項目は、**項目名**(例: 「スポーツ観戦」)と**説明文**(例: 「戦術分析で「なぜこのプレーなのか」を理解しながら見ています」)が分かれて表示され、区切りの「｜」は表示されない(Q1 の決定)。項目名は、活動歴の活動名と同じ大きさと太さの見出しで(Q2 の決定)、説明文はその下に本文の大きさで表示される。4項目は、すべての画面幅で縦一列に並ぶ(Q3 の決定)。
5. 見出し・カードの幅・左端・角丸・内側余白・文字色は、活動歴・コミュニケーション節と揃う(最大幅 672px、左端はヒーローのブロックの左端)。
6. ヒーローと趣味・日常の間、趣味・日常と活動歴の間の余白は、活動歴とコミュニケーション節の間の余白(セクション間の余白)と同じに見える。ヒーローの上側の見え方は変わらない。
7. ヒーロー・活動歴・コミュニケーション節の構成・文言・見た目は変わらない。
8. スマートフォン幅(375px)からデスクトップ幅(1280px)まで、はみ出しや重なりなく読める。

### 支援技術から見えるもの

- 見出しの一覧から、趣味・日常のセクションと、4項目のそれぞれへ移動できる(Q2 の決定)。見出しの階層は `h1`(名前)→ `h2`(趣味・日常)→ `h3`(項目名 4つ)→ `h2`(活動歴)→ `h3`(7つ)→ `h2`(僕のコミュニケーション方法)→ `h3`(3つ)で、飛び越しが無い。ページ全体では `h2` が3つ、`h3` が14個になる(「既存の承認済み AC への影響」節)。
- 4項目は、1つの箇条書きリストの4つの項目として読み取れる(「spec-designer が具体化した点」)。

### 文言の管理(ユーザーには見えないが、本PBIの成果物)

- 見出し・項目名・説明文の文言は、マークアップ(`.astro`)から分けて1か所の定義にまとめる(PBI-004 の約束「文言の管理」を踏襲)。

### ドキュメントの更新(本PBIのタスクに含める)

- [docs/spec/website.md](./website.md) を、Q4 の決定に従って更新する。趣味・日常を第6項「私的な面」の実装として記し、ページ上の並びの記述を更新し、「未決・後続」の「私的な面の具体トピック」を解消済みにする(AC-19)。**この更新は、本PBIのタスクとして行う**(本書の更新時点では、website.md 本体は変更していない)。

### spec-designer が具体化した点(承認済み 2026-09-24)

issue と既存の約束から spec-designer が具体化した点である。質問にはしていないが、PO の承認の対象とし、2026-09-24 に承認された。

- 4項目の並びは issue の記載順(スポーツ観戦 → 印象派展 → のんびり旅 → 読書)とする。
- 見出し「趣味・日常」は、活動歴・コミュニケーション節と同じく、カードの外(ページ背景の上)に置き、文字色は本文色(4.550:1)、大きさと太さは既存の `h2` と同じとする。見出しの下に下線を付けない(PBI-005 の Q2 と同じ)。
- カードの見た目(角丸・内側余白・枠線と影なし)は、既存のカードと同じとする。
- 項目名・説明文の文字色は本文色(白地の上で 6.786:1)とする。基調色は使わない(本文より控えめに見せたい文字が無いため)。
- 4項目は、順序に意味の無い箇条書き(`ul`)の4つの項目(`li`)とし、各項目の項目名と説明文を1つの `li` がまとめて囲む。項目名を見出し(`h3`)にする(Q2 の決定)ため、コミュニケーション節のような行頭記号(黒丸)は付けない。
- アイコン・絵文字・写真・区切り線などの装飾は追加しない(issue に指示が無いため)。
- 趣味・日常の一部が、幅 1280 x 800px のファーストビューに入ることは問題にしない(PBI-005 と同じ考え方)。
- website.md の更新では、「ページ構成(セクション)」の項の番号(1〜7)を振り直さない(PBI-005 と同じ理由。完了済みの仕様が番号で参照しているため)。

### 本PBIでやらないこと

- 項目の追加・削除・並べ替え、文言の要約や言い換え、句点の追加、リンクの追加。
- アイコン・絵文字・写真などの装飾、新しい色トークンの追加。
- 項目ごとのカード(カードは1枚。issue)。
- ヒーロー・活動歴・コミュニケーション節の構成・文言・見た目の変更。セクション間の余白の大きさの変更。
- website.md の第2・5・7項(価値観/About、アウトプット、コンタクト)、第4項の「これから社会人としてのスタンス」の実装。
- ページ内ナビゲーション、アニメーション、ダークモード、多言語対応。

## 受入基準(承認済み 2026-09-24)

> 検証方法の凡例 — `自動テスト`: CI/検査コマンドで機械的に判定できる。`AI実行`: 開発者エージェントがローカルでコマンドを実行し、出力やファイル内容を観測して判定できる。`PO確認`: ブラウザでの見た目・操作の確認を PO が行う。
>
> 〔決定 Q<n>〕は、その AC が PO の決定 Q<n>(2026-09-24。「PO の決定」節)に基づくことを示す。Q1〜Q4 はすべて決定済みで、本節に未決の質問を前提とする AC は無い。

### 配置と見出しの構造

1. **趣味・日常がヒーローと活動歴の間にある**: ビルド成果物の HTML で、趣味・日常の見出しと4項目の要素が、ヒーローの要素(名前の `h1`、一言、SNS 導線4件の `a` 要素)のすべてより**後ろ**、かつ見出し「活動歴」より**前**に出現する。ヒーローの要素どうしの間、および活動歴・コミュニケーション節の中に、趣味・日常の要素が入り込まない。〔検証: AI実行(`npm run build` の出力 HTML を読む)〕
2. **セクションの見出しが `h2` である**: 趣味・日常の見出しが `h2` 要素で、テキストが「趣味・日常」と完全に一致する。〔検証: AI実行〕
3. **項目名が `h3` で、issue の順に並ぶ**〔決定 Q1・Q2〕: 「趣味・日常」の `h2` と「活動歴」の `h2` の間に、`h3` 要素がちょうど4つあり、文書の順に、テキストが「スポーツ観戦」「印象派展」「のんびり旅」「読書」と完全に一致する。〔検証: AI実行〕
4. **ページ全体の見出しの階層が飛ばない**〔決定 Q2〕: ビルド成果物の HTML の見出し要素を文書の順に並べると、`h1`(1つ。「こーたん（齋藤 光貴）」)→ `h2`「趣味・日常」→ `h3`(4つ)→ `h2`「活動歴」→ `h3`(7つ)→ `h2`「僕のコミュニケーション方法」→ `h3`(3つ)となる。`h2` は3つ、`h3` は14個で、`h4`〜`h6` は無い。本 AC は、PBI-005 の AC-4(ページ全体の見出しの数と階層)を引き継ぐ(「既存の承認済み AC への影響」節。この引き継ぎは、2026-09-24 に PO が了承済み)。〔検証: AI実行〕
5. **4項目が1つのリストの4項目で、項目ごとにまとまっている**: 趣味・日常の4項目が、1つの箇条書きリスト(`ul` 要素)の4つのリスト項目(`li` 要素)になっている。各 `li` は、その項目の項目名と説明文をまとめて囲み、他の項目の項目名・説明文を含まない。趣味・日常の `ul` はちょうど1つである。〔検証: AI実行〕

### 文言

6. **文言が issue と1文字も違わない**〔決定 Q1〕: 各項目の `li` の中に、その項目の項目名(issue の表記の「｜」より前)と説明文(「｜」より後ろ)が、それぞれ完全に一致する文字列(かぎかっこ、読点を含む。句点を足さない)で1つずつ存在する。「｜」(U+FF5C)は表示テキストに含まれない。趣味・日常の表示テキストは、見出し1つ・項目名4つ・説明文4つの計9の文字列だけであり、それ以外の説明文は無い。〔検証: AI実行(出力 HTML のテキストを抜き出して比較する)+ PO確認(文言の定義ファイルと issue 原文を PO が照合する。spec-designer は issue を直接取得していないため)〕
7. **文言が1か所にまとまっている**: 趣味・日常の見出し・項目名・説明文の文言が、`.astro` のマークアップに直書きされず、1か所の定義(Markdown ファイル、または同等のデータ定義のファイル)にまとまっている。同じ文言が複数のファイルに重複して書かれていない(仕様書・コメントを除く)。〔検証: AI実行(`grep` で出現箇所を数える)〕

### レイアウトと見た目

8. **白地のカード1枚に、4項目が縦一列で並ぶ**〔決定 Q3〕: ブラウザの幅 375px・639px・640px・1280px のそれぞれで、次のすべてを満たす。〔検証: PO確認(必要に応じて開発者ツールで背景色の算出値を見る)〕
   1. 4項目のすべてが、**1枚の**白地のカード(背景色の算出値が `rgb(255, 255, 255)`)の上にある。項目ごとに別のカードに分かれていない。
   2. 見出し「趣味・日常」は、カードの外(カードより上。ページ背景の上)にあり、見出しの下に下線(線・帯などの装飾)が無い。
   3. 4項目が縦一列で(横に並ぶ項目が無い)、上から issue の順に並ぶ。各項目では、項目名の下に説明文がある(項目名と説明文が横に並ばない)。
   4. 項目と項目の間に、項目の区切りが分かる間隔がある。カードの内側に余白があり、文字がカードの縁に接していない。
9. **文字の大きさと太さ**〔決定 Q2〕: ブラウザの幅 375px と 1280px のそれぞれで、次のすべてを満たす。〔検証: PO確認(開発者ツールで算出値を見る)〕
   1. 趣味・日常の見出し(`h2`)の文字サイズと太さの算出値が、「活動歴」の `h2` と等しい。
   2. 項目名(`h3`)の文字サイズと太さの算出値が、活動歴の活動名(`h3`)と等しい。
   3. 説明文の文字サイズの算出値が、活動歴の本文と等しい。
10. **文字色とコントラスト**: 見出し・項目名・説明文の文字色は、本文色トークン `--color-text-body`(`#4f5984`)である。直下の背景とのコントラスト比(WCAG 2.x の相対輝度式)は、見出し(ページ背景の上)4.550:1、項目名・説明文(白地の上)6.786:1 で、いずれも 4.5:1 以上である。〔検証: AI実行(コンポーネントが参照する色トークンを読み、採用した色の16進値から計算する)、PO確認(開発者ツールで `color` の算出値が `rgb(79, 89, 132)` であることを見る)〕
11. **ヒーロー・既存セクションと幅・左端が揃う**: ブラウザの幅 375px・640px・1024px・1280px で、趣味・日常の見出しとカードの左端が、ヒーローのブロックの左端(= 顔写真の円の左端)、活動歴、コミュニケーション節の左端に揃い、カードの幅が 672px を超えない。幅 1280px では、カードの幅が活動歴のカードの幅と等しい。〔検証: PO確認(必要に応じて開発者ツールで位置と幅を見る)〕
12. **各幅で崩れない**: ブラウザの幅を 375px・639px・640px・1280px にしたとき、趣味・日常のいずれの要素(見出し、カード、項目名、説明文)も画面外にはみ出さず、横スクロールバーが出ず、文字が重ならずに読める。説明文は折り返してよい。〔検証: PO確認〕

### 余白

13. **セクション間の余白が揃い、ヒーローの上側は変わらない**: ブラウザの幅 375px と 1280px(高さ 800px)で、次のすべてを満たす。〔検証: PO確認(必要に応じて開発者ツールで位置を見る)〕
    1. 次の3つの余白が、同じ大きさに見える: ①ヒーローのブロック(SNS 導線4件の下端)から「趣味・日常」の見出しまで、②趣味・日常のカードの下端から「活動歴」の見出しまで、③活動歴のカードの下端から「僕のコミュニケーション方法」の見出しまで(PBI-005 の約束)。
    2. ③の余白の大きさが、本PBIの変更前(ベースブランチ `develop` のビルド、または変更前の公開URLの表示)と変わっていない。
    3. ヒーローの上側(ページの上端から、ヒーローのブロックの上端まで)の見え方が、変更前と変わっていないと PO が判定する。

### 回帰

14. **ヒーローが変わっていない**: ビルド成果物の HTML で、PBI-003 の AC-4(顔写真の `alt`)、AC-9 のうち `h1` の文言、AC-12(SNS 4件の `href`)、AC-14(`target`・`rel`)が引き続き満たされている。PBI-003 AC-9 のうち**ヒーローの一言の文言は、本 AC の判定の対象外**とする(PO 決定 2026-09-24。現在の一言は PBI-005 のときに PO が変更済みで、本PBIは一言に触れない。PBI-005 でも対象外だった)。ブラウザで、PBI-003 の AC-7・AC-8・AC-10(1280 x 800px でヒーローの全要素がスクロールせずに見える)・AC-11 が引き続き満たされている。`src/components/SnsLinks.astro`、`src/data/sns-links.ts`、`src/assets/` に変更が無い。趣味・日常の一部がファーストビューに入ることは問題にしない。本 AC は PBI-005 の AC-16・AC-17 を引き継ぐ。〔検証: AI実行(HTML・差分を読む)+ PO確認(見た目)〕
15. **活動歴とコミュニケーション節が変わっていない**: `src/data/activities.ts`、`src/data/communication.ts`、`src/components/ActivityTimeline.astro`、`src/components/CommunicationSection.astro` に変更が無い。ビルド成果物の HTML で、PBI-005 の AC-3(活動名の `h3` 7つの文言と順序)・AC-5〜AC-7、PBI-005 の AC-18 が引き継いだ PBI-004 の判定(コミュニケーション節の `h3` 3つ、本文9項目、`ul` 3つ・各 `li` 3つ)が引き続き満たされている。〔検証: AI実行(HTML・差分を読む)〕
16. **色トークンを追加・変更していない**: `src/styles/global.css` の4トークンの名前と値に変更が無く、色のトークンの追加が無い。趣味・日常のコンポーネントは色(文字色、カードの背景色)をトークン経由で参照し、16進値などの色のリテラルを直書きしていない。〔検証: AI実行(変更差分を読む。ビルド済み CSS にクラスがあることを使用の証拠にしない)〕
17. **新しい依存を追加していない**: `package.json` の `dependencies` / `devDependencies` に追加が1件も無い。〔検証: AI実行(変更差分を読む)〕
18. **検査コマンドが成功する**: `npm run lint`、`npm run check`、`npm run build` が、いずれも終了コード 0 で完了する。〔検証: AI実行〕

### ドキュメント

19. **website.md が実装と整合している**〔決定 Q4〕(本PBIのタスクとして更新する): [docs/spec/website.md](./website.md) が、次のすべてを満たす。〔検証: AI実行(該当箇所を読む)〕
    1. 「ページ構成(セクション)」第6項「私的な面」に、趣味・日常のセクション(見出し「趣味・日常」、4項目を白地のカード1枚に載せること、各項目が項目名と説明文で構成されること)が、第6項の実装として記されている。4項目の文言は転記しない(確定文言は本書)。
    2. 趣味・日常が、ヒーローの後、活動歴の前に置かれることが読み取れる。第4項の「ページ上の並びは、上から ヒーロー → 活動歴 → 僕のコミュニケーション方法」の記述が、趣味・日常を含む並びに更新されている。項の番号(1〜7)は振り直さない。
    3. 「未決・後続」の「私的な面の具体トピック」が、解消済み(本PBIの4項目)として更新されている。
    4. 上記以外の項・節(「トーン・ビジュアル」を含む)は変更されていない。
    5. 「変更理由」節に、本PBI(issue #7)による更新であることが記録されている。
20. **README が最新である**: [README.md](../../README.md) を見直し、本PBIの変更によって古くなる記述が無いこと(または更新されていること)を確認済みである。〔検証: AI実行〕
21. **公開後も同じ見た目である**: `main` への反映後、GitHub Actions のワークフローが成功し、公開URL `https://kotan-86.github.io/kotan-website/` で AC-8〜AC-13 と同じ見た目が確認できる。〔検証: PO確認〕

### 自動テストの扱い

本PBIの変更は、確定した文言(見出し1つ、項目名・説明文 各4件)の静的な表示であり、条件分岐や計算などの動的なロジックを含まない見込みである。よって **ユニットテストは作らない**([CLAUDE.md](../../CLAUDE.md) の方針。PBI-004・PBI-005 と同じ)。自動検証の主体は AC-18(検査コマンドの成功)、機械的判定の主体は AC-1〜AC-7・AC-10・AC-14〜AC-17・AC-19 とする。issue の表記(「｜」を含む1文字列)から項目名と説明文を分ける処理を自作する場合は、動的なロジックに当たりうるため、その単位でユニットテストを追加し、テスト実行コマンドを [CLAUDE.md](../../CLAUDE.md) に追記する(項目名と説明文を最初から別々に定義すれば、この処理は不要)。

## 既存の承認済み AC への影響(PO の判断材料)

完了済みの PBI-001〜PBI-005 の仕様は凍結し、本文を書き換えない(PBI-005 の Q8 と同じ扱い)。本書の AC で引き継ぐ。

**本書の承認によって PO が了承した事項(了承済み 2026-09-24)**: Q2 の決定(項目名を `h3` にする)により、ページ全体の `h2` が3つ、`h3` が14個になる。これにより、PBI-005 の AC-4(`h2` は2つ、`h3` は10個)は、ページ全体を対象とする文字どおりの判定では成り立たなくなる。本PBI以降は、本書 AC-4 で判定する。Q2 の決定は、この了承を含むものとしては扱わなかった(2026-09-24 の質問は項目名の扱いを問うもので、PBI-005 AC-4 の置き換えの了承を問うていないため)。本書の承認時に、あわせて PO の了承を得た(2026-09-24。AskUserQuestion「承認する」)。

| 既存の AC | 内容(要約) | 本PBIの変更との関係 | 扱い(承認済み 2026-09-24) |
| --- | --- | --- | --- |
| PBI-005 AC-1 | 活動歴がヒーローの要素より後ろ、コミュニケーション節より前 | 間に趣味・日常が入っても成り立つ | 変更なし。本書 AC-1 で趣味・日常の位置を判定 |
| PBI-005 AC-3 | 活動歴の `h2` とコミュニケーション節の `h2` の間に `h3` が7つ | 成り立つ | 本書 AC-15 で引き継ぐ |
| PBI-005 AC-4 | ページ全体で `h2` 2つ・`h3` 10個 | **文字どおりには成り立たなくなる**(Q2 の決定により確定) | 本書 AC-4 で引き継ぐ(PO 了承済み 2026-09-24) |
| PBI-005 AC-15 の1・2 | ヒーローと活動歴の間の余白が、セクション間の余白と同じぐらい | ヒーローの直後が趣味・日常になり、ヒーローと活動歴は隣り合わなくなる | 本書 AC-13 の1で、ヒーローと直後のセクションの間の余白として引き継ぐ(PBI-005 の約束「ヒーローの余白」) |
| PBI-005 AC-16(PBI-003 AC-4・AC-9・AC-12・AC-14) | ヒーローの文言とリンク | 本PBIはヒーローに触れない。一言は PBI-005 のときに PO が変更済みで、PBI-003 AC-9 の一言の文言とは一致しない | 本書 AC-14 で引き継ぐ。ただし PBI-003 AC-9 のうち一言の文言は判定の対象外(PO 決定 2026-09-24。PBI-005 でも対象外だった) |
| PBI-005 AC-15 の4、AC-17 | ヒーローの上側の見え方、ファーストビュー | ヒーローの上側の空きは後続の内容に依存しない見込み(未確認: コードを読んだ推測) | 本書 AC-13 の3・AC-14 で引き継ぐ |
| PBI-005「後続PBIへ残す約束」のセクションの順序 | ヒーロー → 活動歴 → コミュニケーション | issue #7 の指示により ヒーロー → 趣味・日常 → 活動歴 → コミュニケーション になる | 本書「後続PBIへ残す約束」で更新する |

## PO の決定(Q1〜Q4。2026-09-24 決定)

2026-09-24、main が AskUserQuestion で PO に Q1〜Q4 を提示し、PO が回答した。4問とも、PO は推奨案 (a) を選んだ。spec-designer は PO と直接話していない(main から受領した決定の内容による)。issue で明確な点(見出し「趣味・日常」、文言を変えないこと、ヒーローの下・活動歴の上、白地カード1枚)は質問にしていない。

以下の選択肢の表は、決定の経緯の記録として残す(採用しなかった選択肢は、本PBIの対象外)。

### Q1: 「｜」の表示(項目名と説明文の区切り方)

issue の各項目は「項目名｜説明文」の1文字列である。「文言は文字を変えない」の指示に対し、「｜」が**表示する文字**なのか、**項目名と説明文を分ける記法**なのかが、issue からは決まらなかった。

**PO の決定: (a)。** 項目名と説明文を分けて表示し、「｜」は表示しない。項目名・説明文の文字は1文字も変えない。→ AC-3・AC-6

| 選択肢 | 見え方 | トレードオフ |
| --- | --- | --- |
| (a) 項目名と説明文を分けて表示し、「｜」は表示しない(**推奨。PO が採用**) | 「スポーツ観戦」(見出しの大きさ・太さ)の下に、説明文を本文の大きさで表示 | 活動歴(活動名 + 本文)と見た目が揃い、4つの話題を拾い読みしやすい(会話のきっかけという Why に合う)。「｜」を記法とみなすため、「文字を変えない」の解釈として PO の確認が要る。項目名・説明文の文字自体は1文字も変えない |
| (b) issue の表記のまま、「｜」を含めて1行の文として表示 | 「スポーツ観戦｜戦術分析で…見ています」を箇条書きの1項目として表示(コミュニケーション節の箇条書きに近い) | 文字をまったく変えない。項目名が強調されないため、話題の拾い読みはしにくい。項目名を見出しにできない(Q2 は不要になる) |
| (c) 1行の文として「｜」も表示しつつ、項目名だけを太字にする | 「**スポーツ観戦**｜戦術分析で…」 | 文字を変えずに項目名が目立つ。項目名を見出し(`h3`)にすると「｜」の扱いが不自然になるため、見出しにはしない想定 |

### Q2: 項目名の扱い(Q1 が (a) の場合)

**PO の決定: (a)。** 項目名は `h3` とし、大きさ・太さは活動歴の活動名と同じにする。→ AC-3・AC-4・AC-9。ページの `h3` が14個になる件(PBI-005 AC-4)の了承は、本書の承認時に別途得た(2026-09-24。「既存の承認済み AC への影響」節)。

| 選択肢 | トレードオフ |
| --- | --- |
| (a) 項目名を `h3` とし、大きさ・太さは活動歴の活動名・コミュニケーション節の `h3` と同じ(**推奨。PO が採用**) | PBI-004 の約束(セクションの下のまとまりは `h3`)と既存の見た目に揃う。見出しの一覧から各項目へ移動できる。ページの `h3` が14個になり、PBI-005 AC-4 の了承が要る |
| (b) 項目名を `h3` とし、既存の `h3` より一段小さくする | カード1枚に短い4項目という小さなまとまりに合わせて控えめにできる。既存セクションと見出しの大きさが揃わない |
| (c) 項目名を見出しにせず、太字の文字にする | 見出しの数が増えない(`h3` は10個のまま)。見出しの一覧から項目へは移動できない。PBI-004 の約束(まとまりは `h3`)の例外になる |

### Q3: 4項目の並べ方

**PO の決定: (a)。** すべての画面幅で縦一列。各項目は、項目名の下に説明文を置く。→ AC-8

| 選択肢 | トレードオフ |
| --- | --- |
| (a) すべての幅で縦一列。各項目は項目名の下に説明文(**推奨。PO が採用**) | コミュニケーション節と同じ縦の流れで、画面幅による切り替えが無く、狭い幅でも崩れにくい。縦に長くなる(4項目で、ヒーローと活動歴の間が伸びる) |
| (b) 幅 640px 以上は 2列 × 2段、639px 以下は縦一列 | 広い幅で縦がコンパクトになる。1列あたり約 300px になり、説明文(27〜32文字)が2〜3行に折り返す。切り替えの確認箇所が増える |
| (c) 幅 640px 以上は 左に項目名・右に説明文の横並び、639px 以下は縦積み(活動歴と同じ構造) | 活動歴の並びと揃う。項目名の列幅の分だけ説明文の幅が狭まる。切り替えの確認箇所が増える |

### Q4: website.md での位置づけ

**PO の決定: (a)。** website.md 第6項「私的な面」の実装とし、「未決・後続」の「私的な面の具体トピック」を解消済みにする。website.md 本体の更新は、本PBIのタスクとして行う(「ドキュメントの更新」節)。→ AC-19

| 選択肢 | トレードオフ |
| --- | --- |
| (a) 第6項「私的な面」の実装とし、「未決・後続」の「私的な面の具体トピック」を解消済みにする(**推奨。PO が採用**) | 第6項の「趣味・興味・話しやすい話題」と issue の Why(当たり障りのない話題で会話のきっかけを作る)が一致する。「公開目標」の「私的な面は簡易版でも可、後から厚くする」に沿い、今後の追加は別の PBI で行える |
| (b) 第6項のうち「趣味」の実装とし、「興味・話しやすい話題」は未実装として、未決も残す | 第6項の残りが明示される。何を足せば完了なのかが決まっていないため、未決が残り続ける可能性がある |

## 制約・関連機能

### 変更してよい範囲

| 対象 | 扱い |
| --- | --- |
| `src/pages/index.astro` | **主な変更対象。** ヒーローと活動歴の間に趣味・日常を置く。冒頭の仕様コメントに本書への参照を加える。ヒーローのラッパー要素とブロック内部、`main` の余白の指定は変更しない(AC-13・AC-14) |
| 趣味・日常のコンポーネント(新規ファイル) | 追加してよい |
| 文言の定義(新規ファイル。Markdown または同等のデータ) | 追加する(AC-7)。置き場所と形式は実装判断(`src/data/activities.ts`・`src/data/communication.ts` と同じ方式が目安) |
| `src/components/ActivityTimeline.astro`、`src/components/CommunicationSection.astro`、`src/data/*.ts`(既存)、`src/components/SnsLinks.astro`、`src/assets/` | 変更しない(AC-14・AC-15) |
| `src/styles/global.css` | 変更しない(AC-16) |
| `src/layouts/BaseLayout.astro`、`astro.config.mjs`、`package.json` | 変更しない見込み。依存を追加しない(AC-17) |

### 影響を受けるドキュメント

| ドキュメント | 扱い | 順序の制約 |
| --- | --- | --- |
| [docs/spec/website.md](./website.md)「ページ構成(セクション)」第4項・第6項、「未決・後続」、「変更理由」 | Q4 の決定に従って更新する(AC-19)。**本PBIのタスクに含める**(担当のタスクはタスク分割で決める) | 先行更新の制約は無い(色トークンを追加しないため、PBI-002 の「配色を変える手順」は適用されない)。PBI の完了までに実装と矛盾しないことを確かめる |
| [README.md](../../README.md) | 見直す(AC-20)。現状、セクション構成の記述は無い | 無し |
| [docs/spec/pbi-001-site-exists.md](./pbi-001-site-exists.md)〜[docs/spec/pbi-005-activity-timeline.md](./pbi-005-activity-timeline.md) | **凍結**。本文・AC を書き換えない | ― |
| [CLAUDE.md](../../CLAUDE.md) | 変更しない見込み(ユニットテストを追加した場合のみ追記。「自動テストの扱い」) | ― |

### 既存の技術スタックの制約(変更不可)

PBI-001 の「技術スタックの制約」をそのまま引き継ぐ。Astro(TypeScript)+ Tailwind CSS v4、静的サイト出力、GitHub Pages(`base: '/kotan-website/'`)、Node.js 22(LTS)以上。

### 先行PBIから引き継ぐ共有の約束

| 共有物 | 本PBIでの守り方 |
| --- | --- |
| カラートークン(PBI-001 / PBI-002 / PBI-004) | 4トークンの名前と値を変えない。新しい色トークンを足さない。`.astro` に16進値を直書きしない(AC-16) |
| コントラストの下限 4.5:1(PBI-002) | 見出し・項目名・説明文に適用する(AC-10) |
| ヒーローの構成(PBI-003) | 壊さない(AC-14) |
| 見出しの階層(PBI-004) | セクションは `h2`、項目は `h3`(AC-2〜AC-4。Q2 の決定) |
| 見出しの文字色(PBI-004) | ページ背景の上の見出し「趣味・日常」は本文色(AC-10) |
| カードの背景色(PBI-004) | `--color-card-background` を参照し、カードの上の文字は本文色(AC-8・AC-10) |
| 文言の管理(PBI-004) | マークアップから分けた1か所の定義に置く(AC-7) |
| セクション間の余白、ヒーローの余白(PBI-005) | ヒーローと趣味・日常の間、趣味・日常と活動歴の間を、既存のセクション間の余白と同じにする。ヒーローの上側の見え方を保つ(AC-13) |
| 検査コマンド(PBI-001) | `npm run lint` / `npm run check` / `npm run build`(AC-18) |

### 本PBIが後続PBIへ残す約束(承認済み 2026-09-24)

| 共有物 | 約束 |
| --- | --- |
| セクションの順序 | ヒーロー → 趣味・日常 → 活動歴 → 僕のコミュニケーション方法(PBI-005 の約束を更新)。後続の「価値観/About」などの位置は、そのPBIで決める |
| 項目名と説明文の組 | 後続のセクションで「項目名 + 説明文」の組を並べる場合は、本PBIの形に揃える: 項目名は `h3` で活動歴の活動名と同じ大きさ・太さ、説明文は項目名の下に本文の大きさ、区切りの記号(「｜」など)は表示しない(Q1・Q2・Q3 の決定) |

### 設計メモ(実装者への申し送り。実装方法の指定ではない)

- **PBI の大きさ**: [CLAUDE.md](../../CLAUDE.md) の基準は「PBI全体で3コミット程度、1コミットあたりの実装コード変更 300 行まで」。本PBIの実装は、コンポーネント1つ、文言の定義1つ、`index.astro` への組み込み(数行)が中心で、1〜2コミット・合計100行程度に収まる見込みである。余白の調整は、既存のセクション間の余白をそのまま使う想定で、新たな調整は要らない見込み(未確認: コードを読んだ推測。PO 確認で判定)。website.md の更新は行数に数えない。
- **文言の一致(AC-6)**: 項目名・説明文には、かぎかっこ「」、読点「、」が含まれる。説明文の末尾に句点を足さない。Q1 の決定により、「｜」を表示テキストに残さない(区切りの前後に空白を足さない)。
- **色トークンの検証(AC-16)**: Tailwind CSS v4 の自動ソース検出は仕様書やコメントも走査しうるため、ビルド済み CSS にクラスがあることを、コンポーネントがそれを使っている証拠にしない(PBI-005 の feasibility F-3)。新規ファイルのコメントに16進の色の値を書かない。
- **ヒーローの上側(AC-13 の3・AC-14)**: PO 確認の前に、開発サーバで、幅 375px と 1280 x 800px のそれぞれについて、変更前と変更後の上側の見え方を見比べ、1280 x 800px でヒーローの全要素がスクロールなしで見えることを確かめること。
- **アクセシビリティ**: プロダクト全体の基準は未確定([docs/spec/website.md](./website.md)「未決・後続」)。本PBIで求めるのは、見出しの構造(AC-2〜AC-4)、リストとまとまり(AC-5)、コントラスト(AC-10)の範囲に限る。操作要素を置かないため、キーボード操作の新しい要件は無い。

## 変更理由

- issue #7(2026-09-24 起票): スプリント006「hobbies-daily」のリファインメントとして新規作成。issue #7 の Why / What(見出し「趣味・日常」、白地カード1枚に4項目、文言を変えない、ヒーローの下・活動歴の上)を、main 経由で受領した本文に基づいて仕様化した。現行実装(ページの並び、`main` の gap によるセクション間の余白、既存セクションの見た目、見出しの構造、色トークン)を、ファイルと既存のビルド成果物の読み出しで確認した(ブラウザでの表示は未観測)。「｜」の文字コード(U+FF5C。main の転記)とコントラスト比は、一時領域のスクリプトで確認した。PBI-005 の確定版にセクション間の余白の値の記録が無いことを前提節に記した。「｜」の表示(Q1)、項目名の扱い(Q2)、並べ方(Q3)、website.md での位置づけ(Q4)を未決の質問として起票した。受入基準 AC-1〜AC-21 は、各質問の推奨案を仮に置いた案(承認待ち)とした。
- issue #7(2026-09-24、PO の決定 Q1〜Q4 の反映): PO が AskUserQuestion(main 経由)で Q1〜Q4 に回答し、4問とも (a) を選んだ。Q1: 項目名と説明文を分けて表示し「｜」は表示しない(文字は変えない)。Q2: 項目名は `h3`、活動歴の活動名と同じ大きさ・太さ。Q3: すべての幅で縦一列、項目名の下に説明文。Q4: website.md 第6項「私的な面」の実装とし、「私的な面の具体トピック」を解消済みにする。これに従い、「前提」の POの意図に出典3を追加し、「PO への確認」節を「PO の決定」節に改め、What・受入基準・約束の「推奨案」「〔Q<n>〕」の仮置きの表記を決定済みの表記に改めた。AC-8 の3に「横に並ばない」ことを明記し、website.md の更新を本PBIのタスクとして明記した(website.md 本体は未変更)。決定の内容は推奨案と同じため、AC の判定内容は変わらない。承認状態は、この時点では「PO 承認待ち」(受入基準、「spec-designer が具体化した点」、「後続PBIへ残す約束」、PBI-005 AC-4 の置き換えの了承は未承認)。
- issue #7(2026-09-24、PO の承認の反映): PO が AskUserQuestion(main 経由)で「承認する」を選び、受入基準 AC-1〜AC-21、「spec-designer が具体化した点」、PBI-005 AC-4(h2 2つ・h3 10個)の本PBI AC-4(h2 3つ・h3 14個)への置き換え、「本PBIが後続PBIへ残す約束」を承認・了承した。冒頭の承認状態と、各節見出し・表の「承認待ち」「案」「了承待ち」「未了承」の表記を、承認済み(2026-09-24)に更新した。ACの内容・本文の意味は変えていない。website.md 本体は未変更(AC-19 は本PBIのタスクで行う)。
- issue #7(2026-09-24、PO の決定の反映: AC-14 の一言): PO が AskUserQuestion(main 経由)で、AC-14 のうちヒーローの一言(PBI-003 AC-9 の一言の文言)の判定を本PBIでも対象外とすることを決定した(現在の一言は PBI-005 のときに PO が変更済みで、本PBIは一言に触れない。PBI-005 でも対象外だった)。AC-14 の該当文と「既存の承認済み AC への影響」の表に反映した。AC-14 の他の判定(PBI-003 AC-4・AC-9 の `h1`・AC-12・AC-14、AC-7・AC-8・AC-10・AC-11、`SnsLinks.astro` 等の無変更)は変えていない。
